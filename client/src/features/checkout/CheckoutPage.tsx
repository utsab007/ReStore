import { Typography } from "@mui/material"

const CheckoutPage = () => {
  return (
    <Typography variant="h3">
      Only logged in users should able to see this!x
    </Typography>
  )
}

export default CheckoutPage